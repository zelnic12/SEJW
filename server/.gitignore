node_modules/
npm-debug.log*
.env

# Runtime data (JSON store) and logs — regenerated on start.
# Note: this is the runtime store at server/data, NOT server/src/data (seed source).
/data/
*.log

# Frontend copied in from the repo root by scripts/copy-frontend.js on every
# start — generated output, not a source of truth.
/public/
